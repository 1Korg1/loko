#include <iostream>
using namespace std;

int main() {
    int N;
    cout << "Введіть число N: ";
    cin >> N;

    int sum = 0;
    for (int i = 1; i <= N; ++i) {
        sum += i;
    }

    cout << "Сума чисел від 1 до " << N << " дорівнює: " << sum << endl;

    return 0;
}
